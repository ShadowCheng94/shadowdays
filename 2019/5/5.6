今日工作：
做组会ppt，论文“Privacy-Preserving Traffic Monitoring with False ReportFiltering via Fog-assisted Vehicular Crowdsensing”
如何过滤司机上传的错误报告？是这篇文章要解决的问题。 过滤错误报告的过程是分为两个部分，首先是在rsu覆盖的范围下的车辆在握手成功后建立一个邻近图，其次是基于信任传播，rsu监测邻近图中节点可能上传的错误报告。图中 的每个节点都会有个初始的信任值，经过每次的
迭代，节点的信任值会更新，并且与事先预设一个初始值，相比较后，rsu可判断节点是否为恶意节点，若是则将交给ta追踪恶意节点的真实身份。这是目前看到对错误报告过滤的处理方法，但是基本是用语言描述的，所以还没有理解其含义，接下来继续做ppt，看这个论文。
