今日工作：
今天学习的是关于群签名的内容。关于群签名的方案，在成员的加入就对成员的身份作了认证，认证通过后才可以加入群；群管理员是被第三方可信的，这里像在雾服务的那
篇以RSU为群管理员，但并没有假设RSU是可信的，并且群通信需要的参数是RSU来生成的，这就很不安全。所以学习一下对群内成员的撤销以及追踪的问题。“Anonymous and
Traceable Group Data Sharing in Cloud Computing”这篇论文，是将群共享的数据外包给云存储，主要看了它的群管理员如何实现追踪和撤销以及一个容错机制，这个机制
说的是，即使群内有一个恶意车辆，也不妨碍生成正确的群密钥。
